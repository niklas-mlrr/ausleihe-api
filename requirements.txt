# Core runtime dependencies (mirror setup.py `install_requires`).
# Optional extras live in setup.py, install them explicitly:
#   pip install -e ".[bestand]"   # + openpyxl  (Excel-Bestands-Tooling)
#   pip install -e ".[dev]"       # + pytest     (Tests)
requests>=2.31.0
python-dotenv>=1.0.0
