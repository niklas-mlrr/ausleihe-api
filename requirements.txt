# Install the package + its runtime deps from setup.py (single source of truth,
# so there's no manual mirror of `install_requires` to drift). Optional extras:
#   pip install -e ".[bestand]"   # + openpyxl  (Excel-Bestands-Tooling)
#   pip install -e ".[dev]"       # + pytest     (Tests)
-e .
