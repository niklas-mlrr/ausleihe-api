# Install the package + its runtime deps from pyproject.toml (single source of
# truth, so there is no manual dependency mirror to drift). Optional extras:
#   pip install -e ".[bestand]"   # + openpyxl  (Excel-Bestands-Tooling)
#   pip install -e ".[dev]"       # + pytest     (Tests)
-e .
